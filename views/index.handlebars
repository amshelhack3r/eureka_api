<section>
    <h1 class="is-size-2" id="prerequisites">Prerequisites</h1>
    <p class="is-size-5">A decent browser </p>
    <p class="is-size-5">A json formatter</p>
</section>
<section>
    <h1 class="is-size-2" id="resources">Resources</h1>
    <p class="is-size-5">Typically this api works with the tables in the database as end point</p>
    <h2 class="is-size-2">Simple usage:</h2>
    <pre>
        <code>mutalldevs.co.ke/api/{table name}</code>
    </pre>
</section>
<section>
    <h1 class="is-size-2" id="routes">Routes</h1>
    <p class="is-size-4 has-text-success has-text-weight-semibold is-italic">
        GET /{tablename}
    </p>
    <pre>
        <code>gets all values for the table</code>
    </pre>
    <p class="is-size-4 has-text-success has-text-weight-semibold is-italic">GET /{tablename}/{column}/{column_value}</p>
    <pre>
        <code>gets values of a table where {column name} = {column value}
        eg mutalldevs.co.ke/api/client/i/kh001 This returns a client whose i is kh001</code>
    </pre>

    <p class="is-size-4 has-text-success has-text-weight-semibold is-italic">GET /{tablename}/job/{job number}</p>
    <pre>
        <code>These are custom queries or as we call them &#39;jobs&#39; you may want an output out of
            This is still in alpha stage so it is buggy</code>
    </pre>

    <p class="is-size-4 has-text-success has-text-weight-semibold is-italic">POST /{tablename}</p>
    <pre>
        <code>Add a new record to table name supplied. In the request body you must supply key-value pairs for the record to be inserted.
            For testing use postman to do post, put and delete requests
        </code>
    </pre>

    <p class="is-size-4 has-text-success has-text-weight-semibold is-italic">PUT /{tablename}/{column}/{record_id}</p>
    <pre>
        <code>Update the record. Supply the new values in the request body Use postman to test your endpoints</code>
    </pre>

    <p class="is-size-4 has-text-success has-text-weight-semibold is-italic">DELETE /{tablename}/{column}/{record_id}</p>
    <pre>
        <code>Delete the record.Use postman to test your endpoints</code>
    </pre>
</section>
<section>
    <h2 class="is-size-2 is-capitalized has-font-weight-bold" id="deployment">deployment</h2>
    <p class="is-size-5">This api can be used in any programming language that supports RESTful principles</p>
</section>
<section>
    <h2 class="is-size-2 is-capitalized has-font-weight-bold" id="built-with">Built With</h2>
    <ul>
        <p class="is-size-5">
            <a href="https://expressjs.com">Express js</a> - The web framework used</p>
        <p class="is-size-5">
            <a href="https://nodejs.org/en/">Node js</a> - The web server used</p>
        <p class="is-size-5">
            <a href="https://www.mysql.com">MySQL</a> - The database used</p>
    </ul>
</section>
<section>
    <h2 class="is-size-2 is-capitalized has-font-weight-bold" id="contributing">Contributing</h2>
    <p class="is-size-5">Please read
        <a href="https://github.com/amshelhack3r/eureka_api">CONTRIBUTING.md</a> for details on our code of conduct, and the process for submitting pull requests to us.</p>
    <h2 class="is-size-2 is-capitalized has-font-weight-bold" id="authors">Authors</h2>
    <ul>
        <li>
            <p class="is-size-5">
                <strong>Samuel Kanyi</strong> -
                <em>Initial work</em> -
                <a href="https://github.com/amshelhack3r">amshelhack3r</a>
            </p>
        </li>
    </ul>
    <p class="is-size-5">See also the list of
        <a href="https://github.com/amshelhack3r/eureka_api/contributors">contributors</a> who participated in this project.</p>
    <h2 class="is-size-2 is-capitalized has-font-weight-bold" id="license">License</h2>
    <p class="is-size-5">This project is licensed under the MIT License - see the
        <a href="LICENSE.md">LICENSE.md</a> file for details</p>
</section>
<section>
    <h2 class="is-size-2 is-capitalized has-font-weight-bold" id="acknowledgments">Acknowledgments</h2>
    <ul>
        <p class="is-size-6 is-italic has-text-dark has-text-weight-semibold">Hat tip to anyone whose i was used</p>
        <p class="is-size-6 is-italic has-text-dark has-text-weight-semibold">Our Saviours STACKOVERFLOW :)</p>
        <p class="is-size-6 is-italic has-text-dark has-text-weight-semibold">NESCAFE coffee *
            <span>for being my best friend</span>
        </p>
    </ul>
</section>